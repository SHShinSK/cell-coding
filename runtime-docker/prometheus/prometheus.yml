global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: cell-worker-organ
    metrics_path: /metrics
    static_configs:
      - targets:
          - worker-organ:8080
        labels:
          stack: cell-coding

  # divide-scale profile + worker-gateway 실행 시 주석 해제
  # - job_name: cell-worker-gateway
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ['worker-gateway:8080']
