{
  "name": "@cell-coding/compiler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cell Coding DSL compiler draft (lexer, parser, checker) · 세포코딩 DSL 컴파일러 초안",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "node --import tsx --test compile.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "^5.7.3"
  }
}
